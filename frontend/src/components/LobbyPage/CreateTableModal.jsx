export default function CreateTableModal({ open, onClose, form, update, onSubmit, busy }) {
  if (!open) return null;
  return <div className="lobby-modal-layer" role="presentation"><button className="lobby-modal-layer__backdrop" type="button" aria-label="Close create table" onClick={onClose} /><form className="lobby-modal" onSubmit={onSubmit}><button className="lobby-modal__close" type="button" aria-label="Close" onClick={onClose}>×</button><p className="type-label">Host Teen Patti</p><h2>Create a table</h2><label>Table name<input maxLength="100" value={form.name} onChange={update("name")} required /></label><label>Entry bet<input type="number" min="1" value={form.bet} onChange={update("bet")} required /></label><label>Maximum players<select value={form.max} onChange={update("max")}>{[2,3,4,5,6,7,8].map((number) => <option key={number}>{number}</option>)}</select></label><button className="primary" disabled={busy}>{busy ? "Creating…" : "Create table"}</button></form></div>;
}
