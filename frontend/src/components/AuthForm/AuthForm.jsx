import "./AuthForm.css";

const PROFILE_FIELDS = [["firstName", "First name", "text"], ["lastName", "Last name", "text"], ["phone", "Phone", "tel"], ["age", "Age", "number"]];

export default function AuthForm({ register, setRegister, form, update, busy, onSubmit }) {
  return <><div className="auth-form__tabs" role="tablist" aria-label="Account access"><button type="button" role="tab" aria-selected={!register} className={!register ? "selected" : ""} onClick={() => setRegister(false)}>Login</button><button type="button" role="tab" aria-selected={register} className={register ? "selected" : ""} onClick={() => setRegister(true)}>Register</button></div><form className="auth-form" onSubmit={onSubmit}><label>Username<input minLength="3" maxLength="50" autoComplete="username" value={form.username} onChange={update("username")} required /></label>{register && <><div className="auth-form__profile-fields">{PROFILE_FIELDS.map(([name, label, type]) => <label key={name}>{label}<input type={type} min={name === "age" ? "18" : undefined} value={form[name]} onChange={update(name)} /></label>)}</div><label>Email<input type="email" maxLength="254" autoComplete="email" value={form.email} onChange={update("email")} required /></label><label>Gender<select value={form.gender} onChange={update("gender")}><option value="">Prefer not to say</option><option>Woman</option><option>Man</option><option>Non-binary</option></select></label><label>Plan<select value={form.plan} onChange={update("plan")}><option value="Standard">Standard</option><option value="Premium">Premium</option></select></label><p className="auth-form__note">Optional profile details are not sent or stored: the current account API supports username, email, and password only.</p></>}<label>Password<input type="password" minLength="8" maxLength="72" autoComplete={register ? "new-password" : "current-password"} value={form.password} onChange={update("password")} required /></label><button className="primary wide" disabled={busy}>{busy ? "Please wait…" : register ? "Create account" : "Enter lobby"}</button></form><p className="auth-form__switch">{register ? "Already have an account?" : "New to Aceverse?"} <button type="button" onClick={() => setRegister(!register)}>{register ? "Login" : "Register"}</button></p></>;
}
